import * as grouping    from './grouping';
import * as quantifiers from './quantifiers';
import * as sequences   from './sequences';

export default Object.assign({}, grouping, quantifiers, sequences);
